{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{% block field %}
    <div>{{ value|bytes(attribute(field_description.options, 'binary')|default(true), attribute(field_description.options, 'redisplay')|default(false)) }}</div>
{% endblock %}
